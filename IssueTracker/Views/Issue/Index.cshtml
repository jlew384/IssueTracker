@using System.Security.Claims;
@using IssueTracker.Helpers;
@model IssueIndexViewModel

@{
}

<div class="d-flex justify-content-between align-items-center">
    <h1 class="display-4">@Model.ProjectTitle</h1>
    <div>
        @*<select tag="filter-dropdown" class="form-select" asp-for="@Model.Filter" asp-items="@new SelectList(IssueFilter.LIST)"></select>*@
    </div>
    
</div>


<div class="issue-list-container">
    @await Component.InvokeAsync("IssueTable", 
        new {
            projectId = Model.ProjectId,
            filter = Model.Filter,
            sortField = Model.SortField,
            sortDirection = Model.SortDirection
        })
</div>
